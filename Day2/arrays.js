// let coffeeOrder = [
//     "Alex - Cortado, Ben - Cartado, Charlie - Whatever's new"];
// console.log()

let numbers = [1, 2, 3, 4, 5];
 
let doubledNumbers = numbers.map(function(number) {
    return number * 2;
});
 
console.log("Original numbers:", numbers);
console.log("Doubled numbers:", doubledNumbers);
